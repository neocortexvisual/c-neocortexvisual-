#include <iostream>

using namespace std;

int main () {
bool a = true, b = false, res;

// E (AND) => &&
// OU (OR) => ||
// NEGAÇÃO (NOT) => !

res = a || !b;

    cout << boolaplha;
    cout << "Resultado:" << endl;

}