#include <iostream>

using namespace std;

    //struct é tipo um container que ajuda a otimizar funções do código que agrupa dados relacionados
struct Aluno{
    string nome;
    int idade;
    float nota;

    //o construtor coloca as propriedades de valor na estrutura
    Aluno (string nome, int idade, float nota) {
        this->nome = n;
        this->idade = i;
        this->nota = nt;
    }

    void mostrardados () {
        cout << "Informe seu nome: ";
        cin >> aluno.nome;

        cout << "Informe a sua idade: ";
        cin >> aluno.idade;

        cout << "Informe a sua nota: ";
        cin >> aluno.nota;
    }

int main () {
    Aluno turma [3] = {

    for (int i = 0; i < 3; i++) {
        cout << "nome";
        getline (cin, turma [i].nome);

        cin.ignora();
    }
    
}

    return 0;
};
